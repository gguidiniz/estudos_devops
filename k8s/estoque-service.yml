apiVersion: apps/v1
kind: Deployment
metadata:
  name: estoque-service
  namespace: pedidos-veloz
  labels:
    app: estoque-service
spec:
  replicas: 2
  selector:
    matchLabels:
      app: estoque-service
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxSurge: 1
      maxUnavailable: 0
  template:
    metadata:
      labels:
        app: estoque-service
    spec:
      securityContext:
        runAsNonRoot: true
        runAsUser: 1000
      containers:
        - name: estoque-service
          image: ghcr.io/gguidiniz/estoque-service:latest
          ports:
            - containerPort: 5003
          env:
            - name: DATABASE_URL
              valueFrom:
                secretKeyRef:
                  name: postgres-secret
                  key: ESTOQUE_DATABASE_URL
            - name: PORT
              value: "5003"
          envFrom:
            - configMapRef:
                name: services-config
          readinessProbe:
            httpGet:
              path: /health
              port: 5003
            initialDelaySeconds: 5
            periodSeconds: 10
          livenessProbe:
            httpGet:
              path: /health
              port: 5003
            initialDelaySeconds: 10
            periodSeconds: 15
          resources:
            requests:
              cpu: 50m
              memory: 128Mi
            limits:
              cpu: 200m
              memory: 256Mi
---
apiVersion: v1
kind: Service
metadata:
  name: estoque-service
  namespace: pedidos-veloz
spec:
  selector:
    app: estoque-service
  ports:
    - port: 5003
      targetPort: 5003
  type: ClusterIP
