apiVersion: apps/v1
kind: Deployment
metadata:
  name: pedidos-service
  namespace: pedidos-veloz
  labels:
    app: pedidos-service
spec:
  replicas: 2
  selector:
    matchLabels:
      app: pedidos-service
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxSurge: 1
      maxUnavailable: 0
  template:
    metadata:
      labels:
        app: pedidos-service
    spec:
      securityContext:
        runAsNonRoot: true
        runAsUser: 1000
      containers:
        - name: pedidos-service
          image: ghcr.io/gguidiniz/pedidos-service:latest
          ports:
            - containerPort: 5001
          env:
            - name: DATABASE_URL
              valueFrom:
                secretKeyRef:
                  name: postgres-secret
                  key: PEDIDOS_DATABASE_URL
            - name: PORT
              value: "5001"
          envFrom:
            - configMapRef:
                name: services-config
          readinessProbe:
            httpGet:
              path: /health
              port: 5001
            initialDelaySeconds: 5
            periodSeconds: 10
          livenessProbe:
            httpGet:
              path: /health
              port: 5001
            initialDelaySeconds: 10
            periodSeconds: 15
          resources:
            requests:
              cpu: 50m
              memory: 128Mi
            limits:
              cpu: 200m
              memory: 256Mi
---
apiVersion: v1
kind: Service
metadata:
  name: pedidos-service
  namespace: pedidos-veloz
spec:
  selector:
    app: pedidos-service
  ports:
    - port: 5001
      targetPort: 5001
  type: ClusterIP
